import React from "react";
import Home from "./Home";

const Hero = () => {
  return (
    <div
      style={{
        backgroundColor: "rgba(0, 0, 0, 0.9)",
        minHeight: "100vh",
      }}
    >
      <Home />
    </div>
  );
};

export default Hero;
